# Frontend production env for the single-VPS same-origin deployment. Copy to
# `.env.production` before `npm run build`. See .env.local.example for the
# full explanation of NEXT_PUBLIC_API_BASE's three modes — this is mode 1.
NEXT_PUBLIC_API_BASE=same-origin
