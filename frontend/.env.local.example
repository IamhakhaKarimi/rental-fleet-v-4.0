# Frontend environment. Copy to `.env.local`.
# Base URL of the FastAPI backend. Dev default below; in production set this to the
# Railway/Render URL (e.g. https://balkan-fleet-api.up.railway.app).
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8001
